═══════════════════════════════════════════════════════════════════════════════
                  WhatsApp Integration - Final Setup Guide
═══════════════════════════════════════════════════════════════════════════════

✅ COMPLETED:
  ✓ Database tables created
  ✓ Admin menu added to header.php
  ✓ Routing added to system/twgofficial.php
  ✓ WhatsApp controller files created
  ✓ Admin panel UI created

═══════════════════════════════════════════════════════════════════════════════

⏳ REMAINING STEPS: 2 آسان مرحلے

Step 1: Add User Registration Hook
Step 2: Add Order Placement Hook


═══════════════════════════════════════════════════════════════════════════════

📱 STEP 1: USER REGISTRATION HOOK

File: admin/controller/clients.php

جہاں نیا client/user create ہوتا ہے، اس کے بعد یہ add کریں:

────────────────────────────────────────────────────────────────────────────────

// اگر آپ کے پاس یہ code ہے:
$stmt = $conn->prepare("INSERT INTO clients ... ");
$stmt->execute([...]);
$newClientId = $conn->lastInsertId();

// اس کے بعد یہ add کریں:

// ╔════════════════════════════════════════════════════════╗
// ║ WhatsApp - Send Registration Message                  ║
// ╚════════════════════════════════════════════════════════╝

require_once __DIR__ . '/../../whatsapp-server/whatsapp-hooks.php';

// Client کی details حاصل کریں
$stmt = $conn->prepare("SELECT client_name, client_phone, client_email FROM clients WHERE client_id = :id");
$stmt->execute([':id' => $newClientId]);
$clientData = $stmt->fetch(PDO::FETCH_ASSOC);

// WhatsApp message بھیجیں
if ($clientData && $clientData['client_phone']) {
    sendWhatsAppRegisterMessage(
        $newClientId,
        $clientData['client_name'],
        $clientData['client_phone'],
        $clientData['client_email']
    );
}

────────────────────────────────────────────────────────────────────────────────


═══════════════════════════════════════════════════════════════════════════════

📦 STEP 2: ORDER PLACEMENT HOOK

File: admin/controller/orders.php

جہاں نیا order create ہوتا ہے، اس کے بعد یہ add کریں:

────────────────────────────────────────────────────────────────────────────────

// اگر آپ کے پاس یہ code ہے:
$stmt = $conn->prepare("INSERT INTO orders ... ");
$stmt->execute([...]);
$newOrderId = $conn->lastInsertId();

// اس کے بعد یہ add کریں:

// ╔════════════════════════════════════════════════════════╗
// ║ WhatsApp - Send Order Message                         ║
// ╚════════════════════════════════════════════════════════╝

require_once __DIR__ . '/../../whatsapp-server/whatsapp-hooks.php';

// Client details حاصل کریں
$stmt = $conn->prepare("SELECT client_name, client_phone FROM clients WHERE client_id = :id");
$stmt->execute([':id' => $clientId]);
$clientData = $stmt->fetch(PDO::FETCH_ASSOC);

// Service details حاصل کریں
$stmt = $conn->prepare("SELECT service_name FROM services WHERE service_id = :id");
$stmt->execute([':id' => $serviceId]);
$serviceData = $stmt->fetch(PDO::FETCH_ASSOC);

// WhatsApp message بھیجیں
if ($clientData && $serviceData && $clientData['client_phone']) {
    sendWhatsAppOrderMessage(
        $newOrderId,
        $clientId,
        $clientData['client_name'],
        $clientData['client_phone'],
        $serviceData['service_name'],
        $quantity,
        $price
    );
}

────────────────────────────────────────────────────────────────────────────────


═══════════════════════════════════════════════════════════════════════════════

🔍 HOW TO FIND THE EXACT LOCATIONS:

1. clients.php میں اگر:

   آپ کو یہ code نظر آئے:
   INSERT INTO clients (col1, col2, ...)
   VALUES (val1, val2, ...)

   تو اس INSERT کے بعد یہ add کریں:
   
   $newClientId = $conn->lastInsertId();
   require_once __DIR__ . '/../../whatsapp-server/whatsapp-hooks.php';
   ...


2. orders.php میں اگر:

   آپ کو یہ code نظر آئے:
   INSERT INTO orders (col1, col2, ...)
   VALUES (val1, val2, ...)

   تو اس INSERT کے بعد یہ add کریں:
   
   $newOrderId = $conn->lastInsertId();
   require_once __DIR__ . '/../../whatsapp-server/whatsapp-hooks.php';
   ...


═══════════════════════════════════════════════════════════════════════════════

✨ EXAMPLES - Real World Scenarios:

SCENARIO 1: Simple INSERT (Most Common)
────────────────────────────────────────

$stmt = $conn->prepare("INSERT INTO clients (name, phone, email) VALUES (?, ?, ?)");
$stmt->execute([$name, $phone, $email]);
$newId = $conn->lastInsertId();

// Add WhatsApp here ↓

require_once __DIR__ . '/../../whatsapp-server/whatsapp-hooks.php';
sendWhatsAppRegisterMessage($newId, $name, $phone, $email);


SCENARIO 2: Multiple SQL Statements
────────────────────────────────────

$stmt1 = $conn->prepare("INSERT INTO clients ...");
$stmt1->execute([...]);
$clientId = $conn->lastInsertId();

$stmt2 = $conn->prepare("INSERT INTO client_settings ...");
$stmt2->execute([...]);

// Add WhatsApp here (after all inserts) ↓

require_once __DIR__ . '/../../whatsapp-server/whatsapp-hooks.php';
$stmt3 = $conn->prepare("SELECT client_phone, client_name FROM clients WHERE id = ?");
$stmt3->execute([$clientId]);
$data = $stmt3->fetch(PDO::FETCH_ASSOC);
sendWhatsAppRegisterMessage($clientId, $data['client_name'], $data['client_phone'], $email);


SCENARIO 3: Using PDO Prepared Statements
──────────────────────────────────────────

$stmt = $conn->prepare("INSERT INTO clients (name, phone) VALUES (:name, :phone)");
$stmt->execute([
    ':name' => $_POST['name'],
    ':phone' => $_POST['phone']
]);
$newId = $conn->lastInsertId();

// Add WhatsApp here ↓

require_once __DIR__ . '/../../whatsapp-server/whatsapp-hooks.php';
sendWhatsAppRegisterMessage($newId, $_POST['name'], $_POST['phone'], $_POST['email']);


═══════════════════════════════════════════════════════════════════════════════

⚠️ IMPORTANT NOTES:

1. Phone Number Format:
   ✓ صحیح: "923365823432" or "92 3365823432"
   ✗ غلط: "(92) 3365-823432" or "92-336-5823432"
   
   Use this to clean phone numbers:
   $phone = preg_replace('/[^0-9]/', '', $phone);

2. Variable Names:
   آپ کے database میں different column names ہو سکتے ہیں:
   - client_name, clients_name, name, username
   - client_phone, phone, number, mobile
   - client_email, email, email_address

3. Location Finding:
   - Search for "INSERT INTO clients" in clients.php
   - Search for "INSERT INTO orders" in orders.php
   - اسی سے پہلے یا بعد میں hook add کریں

4. Error Handling:
   اگر کوئی field missing ہو تو WhatsApp message نہیں بھیجا جائے گا
   یہ ٹھیک ہے - server silently continue کرتا ہے


═══════════════════════════════════════════════════════════════════════════════

✅ VERIFICATION - کیسے جانیں کہ کام کر رہا ہے:

1. Admin Panel میں جائیں:
   http://yoursite.com/admin/whatsapp

2. QR Code scan کریں (اگر پہلے نہیں کیا)

3. Test message بھیجیں

4. اپنے phone پر message آئے گا

5. Admin panel میں "Message Logs" میں message دیکھیں

6. اب نیا user register کریں یا order place کریں

7. خودکار message آئے گا! 🎉


═══════════════════════════════════════════════════════════════════════════════

🎯 CUSTOMIZATION - Messages کو تبدیل کریں:

Admin Panel > WhatsApp میں:

1. "Registration Message Template" box میں اپنا message لکھیں
   استعمال کریں: {{name}}, {{username}}, {{email}}

2. "Order Message Template" box میں اپنا message لکھیں
   استعمال کریں: {{name}}, {{service}}, {{quantity}}, {{price}}, {{order_id}}

3. "Save Settings" دبائیں

مثال:

Registration:
───────────

سلام {{name}} 👋

خوش آمدید ہماری سروس میں!
یہاں آپ کا نیا اکاؤنٹ ہے۔

Username: {{username}}
Email: {{email}}

شروع کریں: [link]
شکریہ!

Order:
──────

سلام {{name}} 🎉

آپ کا آرڈر #{{order_id}} تیاری میں ہے۔

📦 Service: {{service}}
🔢 Quantity: {{quantity}}
💰 Price: {{price}}

وقت: 24-48 گھنٹے

شکریہ!


═══════════════════════════════════════════════════════════════════════════════

🚀 FINAL CHECKLIST - All Done?

□ Database tables بنے (phpMyAdmin میں check کریں)
□ Admin menu دیکھا (Admin panel میں WhatsApp آیا؟)
□ QR code scan کیا (WhatsApp connected ہے؟)
□ Test message بھیجا (Phone پر آیا؟)
□ Registration hook add کیا (clients.php میں)
□ Order hook add کیا (orders.php میں)
□ Messages customize کیے (Admin panel میں)
□ New user register کیا (WhatsApp message آیا؟)
□ New order place کیا (WhatsApp message آیا؟)

سب ✅ ہو گیا تو مزے لیں! 🎉


═══════════════════════════════════════════════════════════════════════════════

💡 TROUBLESHOOTING:

مسئلہ: Admin panel میں WhatsApp menu نہیں آ رہی
حل: 
  - Refresh کریں (Ctrl+F5)
  - Cache clear کریں
  - logout اور login کریں

مسئلہ: Messages نہیں بھیج رہے
حل:
  - WhatsApp connection check کریں (Status دیکھیں)
  - Phone number صحیح format میں ہے؟
  - Server running ہے؟

مسئلہ: QR code نہیں آ رہی
حل:
  - PowerShell میں npm start چلائیں
  - http://localhost:3001/health check کریں

═══════════════════════════════════════════════════════════════════════════════

✅ مکمل ہو گیا! آپ کا WhatsApp system دستیاب ہے۔

کوئی سوال ہو تو پوچھیں! 🚀

═══════════════════════════════════════════════════════════════════════════════
